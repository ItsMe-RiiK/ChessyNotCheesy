name: Build and Release

on:
  push:
    tags:
      - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

permissions:
  contents: write

jobs:
  build:
    name: Build Release
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install System Dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm base-devel git gtk3 opencv qt6-base libxtst curl tar grim slurp zip

      - name: Build Project
        run: |
          make clean
          make
          make setup-stockfish

      - name: Package Release
        run: |
          mkdir -p ChessyNotCheesy_build_tmp
          cp -r Release images themes scripts launcher.sh README.md ChessyNotCheesy_build_tmp/
          mv ChessyNotCheesy_build_tmp ChessyNotCheesy
          zip -r ChessyNotCheesy-linux-x86_64.zip ChessyNotCheesy

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: ChessyNotCheesy-linux-x86_64.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  cleanup:
    name: Cleanup Old Releases
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Delete Older Releases
        uses: dev-drprasad/delete-older-releases@v0.3.3
        with:
          keep_latest: 2
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
